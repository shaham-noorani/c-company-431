<%= form_with(model: platoon) do |form| %>
  <% if platoon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(platoon.errors.count, "error") %> prohibited this platoon from being saved:</h2>

      <ul>
        <% platoon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :leader_id, 'Select User:' %>
    <%= form.select :leader_id, options_for_select(User.order(:id).map { |user| [user.email, user.id] }) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
